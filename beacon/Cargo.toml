[package]
name = "plum-beacon"
version = "0.1.0"
authors = ["The PolkaX Authors"]
edition = "2018"
license = "GPL-3.0"

[build-dependencies]
prost-build = "0.6"

[dependencies]
prost = "0.6"
grpcio = { version = "0.6", default-features = false, features = ["openssl-vendored"] }
